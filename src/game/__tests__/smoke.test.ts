import { describe, expect, it } from 'vitest'

// Placeholder that verifies the Vitest runner is wired up.
// Replaced by real placement/rules/ai/reducer suites as the game logic lands.
describe('test runner', () => {
  it('runs', () => {
    expect(true).toBe(true)
  })
})
